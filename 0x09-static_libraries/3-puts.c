#include "main.h"
#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void _puts(char *str)
int main (void)
{
	puts{"\" programming is like building a multilingual puzzle");
	return{0};
}

